# GPU Topology Map Fixture

Purpose: provide compact B200 and RTX topology evidence for local topology-map rendering tests.

The fixture uses small synthetic raw captures plus parsed `summary.json` fields
shaped like AICR GPU topology output. These fixtures are renderer examples, not
benchmark studies. They must not be cited as system results.
